<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Your pass</title>
    <style media="screen">
      @import url('https://fonts.googleapis.com/css?family=Raleway');

      body {
        font-family: 'Raleway', sans-serif;
        margin: 0;
      }

      header {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 25vh;
        font-size: 2em;
        background-color: #0e86f7;
      }

      h1 {
        color: white;
      }

      footer {
        overflow: hidden;
        text-align: center;
        height: 25vh;
        background-color: #0e86f7;
      }

      #passcode {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 50vh;
        font-size: 12em;
      }

      #username {
        margin-top: 8vh;
        font-size: 4em;
        color: white;
      }

      #time {
        font-size: 2em;
        color: white;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>Juldi <%= passType %></h1>
    </header>
    <div id="passcode">
      <span><%= passCode %></span>
    </div>
    <footer>
      <div id="username">
        <span><%= name %></span>
      </div>
      <div id="time">
        <%= startDate %> - <%= endDate %>
      </div>
    </footer>
  </body>
</html>
